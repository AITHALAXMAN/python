# Q)write a code to take two different co ordinates as a input and the axis wise cummulate ?
class coordinte():
    def__init__(self,x,y)
        self.x_value=x
        self.y_value=y
    def__add__(self,obj):
        first=self.x_valule + obj.x_value
        second=self.y_value + obj.y_value
        return (first,second)
    def__sub__(self,obj):
        first = abs(self.x_value - obj.x_value)
        second = abs(self.y_value - obj.y_value)
        return (first,second)

obj1=coordinate(10,20)
obj2=coordinate(20,30)

    
    
    
